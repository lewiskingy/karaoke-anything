fastapi==0.121.0
# Pinned explicitly (not left to fastapi's transitive range) to guarantee
# the fix for PYSEC-2026-1942, a Range-header DoS in FileResponse -- this
# app serves settings.html via FileResponse at "/".
starlette==0.49.3
uvicorn[standard]==0.35.0

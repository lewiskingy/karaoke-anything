-r requirements.txt
pytest==8.4.1
pytest-asyncio==1.1.0
pytest-cov==7.1.0
pre-commit==4.6.1
httpx==0.28.1

# CPU-only build: the test suite only needs torch for shape/logic checks on
# the vendored/GPU-image model code, never real inference, so it doesn't need
# the CUDA runtime the default PyPI wheel bundles.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.7.1
